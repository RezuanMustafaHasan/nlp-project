PORT=5000
# Optional: inference works without MongoDB; set this to retain request history.
MONGO_URI=mongodb://127.0.0.1:27017/bangla_punctuation
# Optional override when the local virtual environment is elsewhere.
# PYTHON_BIN=C:\path\to\python.exe
